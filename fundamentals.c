#include<stdio.h>
int main()
{
	int x=6,y=3;
    int sum,diff,pro,div;
    sum=x+y;
    diff=x-y;
    pro=x*y;
    div=x/y;
    printf("x:%d\n",x);
    printf("y:%d\n",y);
    printf("sum=%d\n",sum);
    printf("diff=%d\n",diff);
    printf("pro=%d\n",pro);
    printf("div=%d\n",div);
    return 0;
}
