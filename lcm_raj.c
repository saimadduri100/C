#include<stdio.h>
main()
{
	int gcd(int a, int b);
	if(b==0)
		{
			return a;
		}
		else
		{
			return (b,a%b);
		}
		int lcm(int a, int b);
		{
			return (a*b)/gcd(a,b);
		}
		main()
		{
			int num1,num2;
			printf("enter num1 value");
			scanf("%d",&num1);
			printf("enter num2 value");
			scanf("%d",&num2);
			printf("lcm of %d and %d is %d\n",num1,num2,lcm(num1,num2));
		}
	
}
